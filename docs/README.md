Screenshots for the main README:

- **screenshot-main.png** — Full app window: sidebar (repo, preflight, runs), request field, Run button.
- **screenshot-logs.png** — Run selected: task list, task log picker, log viewer, Retry/Cleanup.

Add these here and they’ll show up in the demo table on the front README.
